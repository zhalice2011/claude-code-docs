# ​​*​* 概述

CodeBuddy IDE 深度融合了设计、编码、调试与部署全流程，旨在通过高度自 动化的工作流，为开发者打造丝滑的 AI 协作体验。

## 基础能力

CodeBuddy 覆盖了从创意萌芽到代码上线的全生命周期：

- **智能对话**：即时技术咨询，支持多轮对话与复杂逻辑推导。
- **代码补全**：毫秒级代码预测，深度适配项目私有风格。
- **Figma 转代码**：一键将 UI 设计稿转化为可维护的高质量前端代码。
- **组件库集成**：预置企业级 UI 组件，支持自然语言调整样式与布局。
- **智能诊断**：自动发现潜在 Bug 与性能瓶颈，提供一键修复方案。

## 核心能力

CodeBuddy 代码补全 **Pro**：引入 `NES` (Next Edit Suggestions) 前瞻式编辑能力，重新定义智能补全， [了解更多](./Code-Completion)。

**核心亮点**：超越光标，预见下一步

突破传统补全局限，`NES` 不仅补全当前行，更能预测接下来的编码意图，自动对相关联的后续代码块（如下一行逻辑、函数体结尾）给出智能建议，真正实现 “写一补十” 的前瞻式体验。

**无缝集成**：默认开启，随心触发

功能默认开启，支持自动预判与手动触发双模式。手动触发快捷键：`Ctrl + Shift + Enter`, 一键召唤灵感，让编码行云流水。

---

## 快速开始

### 快捷唤起

| 操作系统 | 快捷键 | 功能描述 | 快捷键 | 功能描述 |
| --- | --- | --- | --- | --- |
| **macOS** | `⌘` \+ `I` | 内联对话 | `Ctrl` \+ `⌘` \+ `I` | 打开侧边栏对话 |
| **Windows** | `Ctrl` \+ `I` | 内联对话 | `Ctrl` \+`Win` \+`I` | 打开侧边栏对话 |

### 基础交互

- **新建会话**：点击顶部的 `+` 图标。
- **上下文引用**：在输入框中输入 `@` 即可引用文件、文件夹或当前代码片段。
- **多模态输入**：支持直接粘贴截图或导入 Figma 链接进行 UI 开发。

## 智能工作流

CodeBuddy 预置了多种专业模式，并支持自定义扩展，以适配不同的开发需求：

### 1\. 核心交互模式

- **Ask (对话模式)**：专注于技术问答与概念解释，不直接修改代码。适合快速获取思路或查阅 API。
- **Craft (Agent模式)**：专注于当前上下文的代码生成与局部修改。响应迅速，适合精准的函数编写或小范围重构。
- **Plan (计划模式)**：**最强自动化模式**。适用于跨文件修改或复杂功能开发。AI 会先制定任务清单（Plan），经您确认后自主执行读取、修改及环境检查。了解更多 [Plan 模式](./../Features/Plan-Mode)。

### 2\. 自定义 Agent

除了预置模式，您还可以点击 **「\+ 创建 Agent」**，根据特定业务逻辑或工作习惯，定制专属的 AI 智能体，实现更高程度的流程自动化。了解更多 [自定义 Agent](./../Features/Subagents)。

## 进阶特性

CodeBuddy 的强大之处在于其可配置与可扩展的“大脑”：

- **Memory (记忆偏好)**：自动记录您的偏好与项目关键背景，让 AI 随时间推移变得越来越精，更多参考[Memory](./Memory)。
- **Rules (规则)**：通过自定义 `.codebuddy/rules`，强制 AI 遵循特定的架构设计或编码规范，更多参考[Rules](./Rules)。
- **Skills (技能库)**：通过用户/项目级配置 `.codebuddy/skills/`, 针对特定场景（如安全审计、性能压测）封装的专业指令集，更多参考[Skills](./../Features/Skills)。
- **MCP (模型上下文协议)**：开放协议，支持接入第三方工具（如数据库、实时搜索）作为 AI 的辅助工具，更多参考[MCP](./MCP)。
- **自定义模型（API接入）**：支持自定义模型（API 接入），实现更丰富的 AI 能力，更多参考[Models](./../Features/models)。
- **自定义指令（个性化适配）**：支持个性化开发习惯，让代码补全贴合个人 / 团队习惯与专属业务场景，更多参考[自定义指令](./Slash-Commands#自定义斜杠指令)。

## 集成与生态 (Integrations)

CodeBuddy 与主流开发生态深度打通，实现一站式研发闭环：

- **云服务集成**：原生支持 CloudBase、Supabase、CloudStudio，实现数据库管理与一键预览部署。
- **前端组件集成**：支持通过选择标准化组件库规范，直接生成符合企业级设计系统的可维护前端代码。
- **设计组件集成**：支持通过 `Add from Figma` 功能按钮从内置的 Figma 中选择设计稿导入到 IDE 中，可准确将 Figma 设计稿转换为生产代码。 [了解更多](./config-integration)

> **小贴士**：您可以根据任务复杂度随时在 **Ask、Craft、Plan** 模式间切换。对于需要大范围改动代码的任务，可以尝试使用 [Plan 模式](./../Features/Plan-Mode)！